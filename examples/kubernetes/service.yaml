apiVersion: v1
kind: Service
metadata:
  name: mcp-gateway
  labels:
    app: mcp-gateway
spec:
  type: ClusterIP  # Change to LoadBalancer or NodePort for external access
  selector:
    app: mcp-gateway
  ports:
  - port: 8811
    targetPort: 8811
    protocol: TCP
    name: mcp
