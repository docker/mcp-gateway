version: 2
name: powertoys
displayName: PowerToys MCP Catalog
servers:
  - name: powertoys
    description: A collection of utilities for power users
    type: server
    image: powertoys-mcp:latest
    transport: stdio
    tools:
      - name: bogus
        description: "This is required by the MCP Gateway and MCP Catalog, but I don't want to have to manage the list manually... Also, what if I wanted a dynamic tool list?"
